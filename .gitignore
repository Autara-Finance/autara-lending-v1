target
.arch_data
.arch_local
app.log
new_txns.txt
new_blocks.txt
authority.key
user.key
.DS_Store
.env

# Key material: never track keypairs. Working copies live on disk only.
keys/
*.key

# Deploy tooling: gitignored key directories (autara-deploy). NEVER commit keys.
.keys-testnet/
.keys-mainnet/
.keys-*/
