target
.arch_data
.arch_local
app.log
new_txns.txt
new_blocks.txt
authority.key
user.key
.DS_Store
.env

# Deploy tooling: gitignored key directories (autara-deploy). NEVER commit keys.
.keys-testnet/
.keys-mainnet/
.keys-*/

# Local-only ops notes (not for the public repo)
docs/PROCEDURES_DEEPANSHU.md
.local-docs/
